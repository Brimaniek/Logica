"""Crea una clase Rectangulo que tenga dos atributos, ancho y alto. 
Incluye un método area que calcule y devuelva el área del rectángulo, 
y un método perimetro que devuelva el perímetro. 
Crea una instancia de Rectangulo y muestra su área y perímetro."""


class Rectangulo:
    def  __init_(self,ancho,alto):
          self. ancho = ancho
          self.alto = alto
          
   
          def  area(self):
              return (" El area del circulo es :")
              
              #Perimetro
          def  perimetro(self):
                return  2 * (self.ancho + self.alto)
              
              
              # Instancia del rectangulo......
                mi_rectangulo = Rectangulo(3,5)
                
      
                
              #Perimetro
          def  perimetro(self):
                perimetro = 2 * (ancho + alto)
                return perimetro
              
              
              # Instancia del rectangulo......
                mi_rectangulo = Rectangulo(3,5)
                
                #Mostrar resultados......
                print(mi_rectangulo.area())
                print(mi_rectangulo.perimetro())
                